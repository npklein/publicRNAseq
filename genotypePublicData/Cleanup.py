import logging
import sys
import os

format = '%(asctime)s - %(levelname)s - %(funcName)s - %(message)s'
logging.basicConfig(stream=sys.stdout, level=logging.INFO,
                    format=format)
class Cleanup:
    def __init__():
        '''Cleanup finished data
        '''
        pass

    def clean(self, sample):
        '''Clean up data for sample'''
        pass